# Local development — Vite proxy handles /api → localhost:3001
# VITE_API_URL is not needed locally

# For production (Vercel), set VITE_API_URL in Vercel dashboard
# Example: VITE_API_URL=https://pashusuraksha-api.railway.app/api
VITE_API_URL=
